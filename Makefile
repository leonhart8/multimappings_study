SHELL := /bin/bash

clean:
	rm -rf arabidopsis_thaliana_index* lambda_index* lambda_index_* lambda_k_* lambda_*_k_* lambda_a_* lambda_*_a_* arabidopsis_k_* arabidopsis_a_* secondary_alignements_* supplementary_alignements_*

hello:
	echo 'hello'
